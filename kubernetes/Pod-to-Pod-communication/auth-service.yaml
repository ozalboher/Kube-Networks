apiVersion: v1
kind: Service
metadata:
  name: users-service
spec:
  selector:
    app: users
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80 # The port to the service itself (usually 80 or 443 for a standard web service)
      targetPort: 80 # The port on the Pod that the service should forward to (what is specified in app.js)